/**
 * Master switch for auto Google Translate on `/if` booking-widget routes.
 * Set to `true` to re-enable; all translate logic stays in place when off.
 */
export const GOOGLE_TRANSLATE_ENABLED = true;
